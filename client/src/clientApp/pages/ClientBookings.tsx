import { Link } from 'react-router-dom';
import { CalendarDays, MapPin } from 'lucide-react';
import { useState } from 'react';
import { useClientBookings } from '../hooks/useClientPortal';
import { Panel, PortalPage, State, statusClass } from './PortalPages';

export default function ClientBookings() { const [search, setSearch] = useState(''); const [status, setStatus] = useState('All'); const { data, isLoading, isError, refetch } = useClientBookings({ search: search || undefined, status }); const bookings = data?.data || []; return <PortalPage title="My events" eyebrow="Your bookings"><div className="flex flex-col gap-3 sm:flex-row"><input aria-label="Search bookings" value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Search event or booking number" className="flex-1 rounded-xl border border-[#5a1e1e]/15 bg-white px-4 py-3 text-sm outline-none focus:border-[#c89b3c]" /><select aria-label="Filter booking status" value={status} onChange={(e) => setStatus(e.target.value)} className="rounded-xl border border-[#5a1e1e]/15 bg-white px-4 py-3 text-sm"><option>All</option><option>Pending</option><option>Confirmed</option><option>Completed</option><option>Cancelled</option></select></div><State loading={isLoading} error={isError} retry={refetch} />{!isLoading && !isError && !bookings.length ? <State empty="No events scheduled yet." /> : <div className="grid gap-5 md:grid-cols-2">{bookings.map((booking: any) => <Panel key={booking.id} className="transition hover:-translate-y-1"><div className="flex items-start justify-between gap-3"><div><p className="text-xs uppercase tracking-[0.18em] text-[#c89b3c]">{booking.bookingNumber}</p><h2 className="mt-2 font-serif text-2xl text-[#5a1e1e]">{booking.eventType}</h2></div><span className={`rounded-full px-3 py-1 text-xs font-semibold ${statusClass(booking.status)}`}>{booking.status}</span></div><div className="mt-5 space-y-2 text-sm text-[#76675b]"><p className="flex gap-2"><CalendarDays size={16} />{new Date(booking.eventDate).toLocaleDateString('en-IN', { dateStyle: 'long' })}</p><p className="flex gap-2"><MapPin size={16} />{booking.venue || 'Venue to be confirmed'}</p><p>{booking.guestCount || 0} guests</p></div><Link to={`/client/bookings/${booking.id}`} className="mt-6 inline-flex rounded-xl bg-[#5a1e1e] px-4 py-2 text-sm font-semibold text-white">View details</Link></Panel>)}</div>}</PortalPage>; }
