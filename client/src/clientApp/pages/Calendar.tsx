import { useClientCalendar } from '../hooks/useClientPortal'; import { Panel, PortalPage, State, statusClass } from './PortalPages';
export default function Calendar() { const { data, isLoading, isError, refetch } = useClientCalendar(); const events = data?.data || []; return <PortalPage title="Event calendar"><State loading={isLoading} error={isError} retry={refetch} />{!isLoading && !isError && !events.length ? <State empty="No events scheduled yet." /> : <Panel><div className="space-y-3">{events.map((event: any) => <div key={event.id} className="flex flex-col gap-3 rounded-2xl border border-[#5a1e1e]/10 p-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="font-serif text-xl text-[#5a1e1e]">{event.title}</p><p className="text-sm text-[#76675b]">{new Date(event.start).toLocaleString('en-IN', { dateStyle: 'long', timeStyle: 'short' })} · {event.venue || 'Venue TBD'}</p></div><span className={`w-fit rounded-full px-3 py-1 text-xs font-semibold ${statusClass(event.status)}`}>{event.status}</span></div>)}</div></Panel>}</PortalPage>; }
